type User {
  userId: ID!
  email: String!
  key: String!
  scrappers: [Scrapper!]!
}

input CreateUserInput {
  email: String!
  key: String!
}

type Query {
  getUser(userId: ID!): User
  getUserList: [User!]!
}

type Mutation {
  createUser(input: CreateUserInput!): User 
}